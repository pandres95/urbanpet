(function () {
    'use strict';

    (angular
        .module('urbanPet.category', ['ui.router'])
        .config(Configure)
    ).run(Run);

    function Configure() {

    }

    function Run() {
    }

})();
